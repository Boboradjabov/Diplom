<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <title>Админ-панель</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body class="bg-dark text-light">

    <div class="container mt-5">
        <h2 class="text-center">Сообщения пользователей</h2>

        {% for msg in messages %}
            <div class="card bg-secondary text-light mb-3 p-3">
                <p><strong>Имя:</strong> {{ msg[1] }}</p>
                <p><strong>Email:</strong> {{ msg[2] }}</p>
                <p><strong>Тема:</strong> {{ msg[3] }}</p>
                <p><strong>Сообщение:</strong> {{ msg[4] }}</p>
            </div>
        {% endfor %}

        <a href="{{ url_for('logout') }}" class="btn btn-warning">Выйти</a>
    </div>

</body>

</html>
